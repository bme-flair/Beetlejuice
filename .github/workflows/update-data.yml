name: Update brightness data

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install R packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::ggplot2
            any::svglite

      - name: Fetch AAVSO observations
        run: python scripts/fetch_aavso.py --lookback-days 3650 --target "alf Ori" --requested-bands "Vis,V" --summary-band "Vis"

      - name: Generate long-term brightness graph
        run: Rscript scripts/plot_brightness.R data/observations.csv data/brightness_plot.svg Vis

      - name: Commit historic data and graph
        run: |
          if [[ -n "$(git status --porcelain data/observations.json data/observations.csv data/daily_brightness.json data/brightness_plot.svg)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/observations.json data/observations.csv data/daily_brightness.json data/brightness_plot.svg
            git commit -m "Update historic brightness data"
            git push
          else
            echo "No historic data changes to commit."
          fi

      - name: Generate recent brightness graph and table
        run: Rscript scripts/plot_recent_brightness.R data/observations.csv data/recent_brightness_plot.svg data/recent_brightness.json Vis 420 21

      - name: Commit recent brightness view
        run: |
          if [[ -n "$(git status --porcelain data/recent_brightness_plot.svg data/recent_brightness.json)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/recent_brightness_plot.svg data/recent_brightness.json
            git commit -m "Update recent brightness view"
            git push
          else
            echo "No recent view changes to commit."
          fi
